const validImageFormats = imageFormat =>  imageFormat === "image/png" || 
                                            imageFormat === "image/jpeg" ||
                                            imageFormat === "image/jpg";

export {
    validImageFormats
};